[workspace]
members = [
    "execution-engine",
    "tokens", 
    "program",
    "zk-proof",
    "execution-server",
    "smt",
]
resolver = "2"

[workspace.dependencies]
alloy-sol-types = "1.0"
borsh = { version = "1.5.7", default-features = false, features = ["derive"] }
